1. check if req.body.tokens are readable on client
2. complete user endpoints w/ authenticated ROUTES
3. rest of endpoints

check how to ON DELETE CASCADE record_entry (they currently don't hold primary keys)